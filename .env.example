# A2I2 Platform Environment Variables
# =====================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
#
# cp .env.example .env.local

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Choose ONE: Supabase OR Neon

# --- Option A: Supabase ---
# Get from: Supabase Dashboard > Project Settings > API
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_ANON_KEY="your-anon-public-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# For Next.js frontend (public, exposed to browser)
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-public-key"

# --- Option B: Neon (Recommended for Vercel) ---
# Get from: Neon Dashboard > Connection Details
# Use POOLED connection for API routes
DATABASE_URL="postgres://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require"
# Use DIRECT connection for migrations only
DATABASE_URL_DIRECT="postgres://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"

# ===========================================
# AI MODEL API KEYS (Required)
# ===========================================

# Anthropic Claude API
# Get from: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY="sk-ant-api03-your-key-here"

# Google Gemini API
# Get from: https://aistudio.google.com/apikey
GEMINI_API_KEY="AIzaSy-your-key-here"

# ===========================================
# AI MODEL CONFIGURATION (Optional)
# ===========================================

# Default Gemini model
# Options: gemini-3-pro-preview, gemini-3-flash-preview, gemini-2.5-pro, gemini-2.5-flash
GEMINI_DEFAULT_MODEL="gemini-3-flash-preview"

# Thinking level for Gemini 3 models
# Options: minimal, low, medium, high
GEMINI_THINKING_LEVEL="high"

# Temperature for model responses
# Range: 0.0-2.0 (Gemini 3 optimized for 1.0)
GEMINI_TEMPERATURE="1.0"

# ===========================================
# EMBEDDINGS & SEARCH (Optional)
# ===========================================

# OpenAI - for text-embedding-3-small/large
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-your-key-here"

# Voyage AI - alternative embedding provider
# Get from: https://www.voyageai.com/api
VOYAGE_API_KEY=""

# Cohere - alternative embedding provider
# Get from: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=""

# ===========================================
# VOICE INTEGRATION (Phase 3+)
# ===========================================

# --- PersonaPlex (Primary Voice System) ---
# Full-duplex, 170ms latency, custom voice
# Requires GPU with 16GB+ VRAM (AWS g5.xlarge, Lambda Labs A10, etc.)

# HuggingFace token for PersonaPlex model access
# Get from: https://huggingface.co/settings/tokens
# Must accept license at: https://huggingface.co/nvidia/personaplex-7b-v1
HF_TOKEN=""

# PersonaPlex server URL (WebSocket)
# Local: wss://localhost:8998
# Cloud: wss://personaplex.your-server.com:8998
PERSONAPLEX_URL="wss://localhost:8998"

# PersonaPlex voice preset
# Options: NATF0-NATF3 (female), NATM0-NATM3 (male), VARF0-VARF4, VARM0-VARM4
# Recommended for Arcus: NATF2 (professional female) or NATM1 (professional male)
PERSONAPLEX_VOICE="NATF2"

# --- Gemini Live API (Fallback Voice) ---
# Used when PersonaPlex is unavailable
# Real-time audio via WebSocket
GEMINI_LIVE_MODEL="gemini-2.5-flash-native-audio-preview"

# --- Wake Word Detection ---
# Options: nanowakeword (free, Apache 2.0) or picovoice (commercial)
WAKE_WORD_ENGINE="nanowakeword"
WAKE_WORD_MODEL="hey_arcus"
WAKE_WORD_SENSITIVITY="0.5"

# Picovoice (alternative, commercial)
# Get from: https://console.picovoice.co/
# Custom wake word training: $100 one-time
PICOVOICE_ACCESS_KEY=""

# --- Alternative Voice (Development/Testing Only) ---
# Use when PersonaPlex GPU is unavailable

# OpenAI cloud voice (simple STT + TTS, not full-duplex)
# VOICE_MODE="cloud"
# VOICE_STT_SERVICE="openai"
# VOICE_TTS_SERVICE="openai"
# VOICE_TTS_VOICE="nova"

# ElevenLabs premium TTS
# Get from: https://elevenlabs.io/app/api-keys
ELEVENLABS_API_KEY=""

# ===========================================
# AUTHENTICATION (Production)
# ===========================================

# Simple API key authentication
# Generate with: openssl rand -base64 32
API_SECRET_KEY="generate-a-secure-random-key-here"

# Clerk Authentication (if using)
# Get from: https://dashboard.clerk.com/
CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

# ===========================================
# DEPLOYMENT CONFIGURATION
# ===========================================

# Node environment
NODE_ENV="development"

# Vercel region (optional)
# Options: iad1, sfo1, etc.
VERCEL_REGION="iad1"

# ===========================================
# MONITORING & ALERTING (Production)
# ===========================================

# Discord webhook for alerts
# Get from: Discord Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL=""

# Resend for email notifications
# Get from: https://resend.com/api-keys
RESEND_API_KEY=""

# Axiom for log aggregation
# Get from: https://app.axiom.co/settings/api-tokens
AXIOM_TOKEN=""
AXIOM_ORG_ID=""

# ===========================================
# MULTI-CHANNEL GATEWAY (ClawdBot-Inspired)
# ===========================================

# --- Gateway Core ---
ARCUS_GATEWAY_HOST="127.0.0.1"
ARCUS_GATEWAY_WS_PORT="18790"
ARCUS_GATEWAY_HTTP_PORT="8080"
ARCUS_DEFAULT_MODEL="claude-sonnet-4-20250514"
ARCUS_MAX_CONTEXT_TOKENS="8000"
ARCUS_MEMORY_INJECTION="true"
ARCUS_AUTO_LEARN="true"
ARCUS_SESSION_TIMEOUT="30"
ARCUS_MAX_SESSIONS="100"
# Comma-separated list to enable only specific adapters (leave empty for all)
# Options: whatsapp,discord,siri
ARCUS_ADAPTERS=""
# Set to "true" for verbose logging
ARCUS_DEBUG="false"

# --- WhatsApp (via Baileys Node.js bridge) ---
# Requires a Baileys bridge process running on this URL
# See: https://github.com/WhiskeySockets/Baileys
WHATSAPP_BRIDGE_URL=""
# Comma-separated phone numbers (E.164 format) allowed to interact
WHATSAPP_ALLOWLIST=""
WHATSAPP_READ_RECEIPTS="true"
WHATSAPP_TYPING_INDICATOR="true"

# --- Discord ---
# Create a bot at: https://discord.com/developers/applications
# Enable MESSAGE_CONTENT intent in Bot settings
DISCORD_BOT_TOKEN=""
# Comma-separated guild (server) IDs to allow (leave empty for all)
DISCORD_GUILD_IDS=""
# Require @mention to trigger (useful in busy servers)
DISCORD_REQUIRE_MENTION="false"
DISCORD_RESPOND_DMS="true"

# --- Siri Shortcuts (Webhook) ---
# API secret for authenticating Shortcut requests
# Generate with: openssl rand -base64 32
SIRI_API_SECRET=""
# Comma-separated device IDs allowed (leave empty for all authenticated)
SIRI_ALLOWED_DEVICES=""
SIRI_WEBHOOK_PATH="/webhook/siri"
SIRI_MAX_RESPONSE_LENGTH="500"

# ===========================================
# INTEGRATIONS (Future)
# ===========================================

# Zapier webhook URL (for workflow automation)
ZAPIER_WEBHOOK_URL=""

# Google Vertex AI (for enterprise deployments)
VERTEXAI_PROJECT_ID=""
VERTEXAI_LOCATION=""
